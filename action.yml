name: 'DeploySlim'
description: 'Recursively compress web assets using Brotli and Gzip for lightning-fast deployments in the current working directory by default.'
author: 'CornerstoneCode'  

inputs:
  directory:
    description: 'The directory containing the web assets to compress.'
    required: false
    default: '.'
  algorithms:
    description: 'Comma-separated list of compression algorithms (br, gz). Default: br,gz'
    required: false
    default: 'br,gz'
  brotli-level:
    description: 'Brotli compression level (0-11). Default: 6'
    required: false
    default: '6'
  gzip-level:
    description: 'Gzip compression level (0-9). Default: 6'
    required: false
    default: '6'

runs:
  using: 'python'
  main: 'src/main.py'